"""Merge LoRA adapter into base model and push to HF Hub.

Stub — implemented in Weekend 4.
"""

from __future__ import annotations

from pathlib import Path


def merge_and_save(base_model: str, adapter_dir: Path, output_dir: Path) -> Path:
    """Merge adapter into the base, save merged weights, return path."""
    raise NotImplementedError("Implemented in Weekend 4.")
